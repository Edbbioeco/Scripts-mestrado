# Pacotes ----

library(sf)

library(tidyverse)

library(leaflet)

library(leaflet.extras)

library(leafem)

library(maptiles)

library(tidyterra)

library(terra)

library(randomForest)

library(ggspatial)

library(ggview)
